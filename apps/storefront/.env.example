API_BASE_URL=http://localhost:4000
# Client-side equivalent of the above - customer auth and checkout run in the
# browser, not in a Server Component, so they need the NEXT_PUBLIC_ prefix to
# be inlined into the client bundle. Same value as API_BASE_URL in local dev.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
