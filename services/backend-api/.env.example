NODE_ENV=development
PORT=4000

# Additional variables (DATABASE_URL, JWT secrets, etc.) are introduced
# in Milestone 0 Step 2 (Database Foundation) and Step 3 (Authentication).
# Not needed for this step.
